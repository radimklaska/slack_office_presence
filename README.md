# Slack office presence
Raspberry Pi scans local network and posts to Slack when someone connects / disconnects.

# Requiremens
* Linux box running in local network
 * arp-scan (sudo apt-get install arp-scan)
 * PHP + curl
* Slack
